{
  "name": "@iagows/tree_example",
  "version": "1.0.0",
  "author": {
    "name": "Iago Alves",
    "url": "https://github.com/iagows"
  },
  "private": true,
  "workspaces": [
    "packages/*",
    "storybook"
  ],
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "devDependencies": {
    "lerna": "^6.4.0"
  },
  "scripts": {
    "build:basicData": "yarn workspace @iagows/basic-data build",
    "build:businessData": "yarn workspace @iagows/business-data build",
    "build:components": "yarn workspace @iagows/components build",
    "build:dataStructure": "yarn workspace @iagows/data-structure build",
    "build": "yarn run build:basicData && yarn run build:dataStructure && yarn run build:businessData && yarn run build:components"
  }
}